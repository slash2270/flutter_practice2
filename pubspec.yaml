name: flutter_practice2
description: A new Flutter project.

# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0" #null safety >= 2.12.0

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  dio: ^4.0.6
  fluttertoast: ^8.0.9
  image_gallery_saver: ^1.7.1
  money2: ^2.3.0 #貨幣金額解析
  sprintf: ^6.0.0 #佔位符
  flustars: ^2.0.1 #常用工具類
  fluro: ^2.0.3 #路由控制
  shimmer: ^2.0.0 #閃光
  #bubble: ^1.2.1 #氣泡對話框
  google_maps_flutter: ^2.1.8
  google_maps_flutter_web: ^0.4.0
  flutter_google_places: ^0.3.0
  #flutter_google_places_hoc081098: ^1.1.0 有更新升級就不用
  html: ^0.15.0
  http: ^0.13.4
  meta: ^1.3.0
  json_annotation: ^4.5.0
  json_serializable: ^6.2.0
  json_model: ^1.0.0
  geolocator: ^8.2.1 #gps
  path_provider: ^2.0.1
  path_provider_macos: ^2.0.0
  provider: ^6.0.2
  uuid: ^3.0.4
  flutter_staggered_grid_view: ^0.6.1
  flutter_slidable: ^1.3.0
  flutter_neumorphic: ^3.0.3 #神經擬態
  animations: ^2.0.3
  event_bus: ^2.0.0
  geocoder: ^0.2.1 #place address
  rxdart: ^0.26.0 #配合map/place
  permission_handler: ^10.0.0 #權限管理器
  before_after: ^2.1.0 #改變之前之後
  like_button: ^2.0.4 #動畫版按鈕
  qr_code_scanner: ^1.0.0 #QRCode
  #battery_plus: ^2.1.4 電池電量
  pk_skeleton: ^0.0.2 #List Shimmer
  #install_plugin: ^2.0.1
  flutter_speed_dial: ^6.0.0 #變化版 fab
  scoped_model: ^1.1.0 #子元件複製父元件模型
  sliding_up_panel: ^2.0.0+1 #可拖動的bottomSheet
  scratcher: ^2.2.1 #刮刮樂
  #video_compress: ^3.1.1 #影片壓縮
  image_picker: ^0.8.5+3
  video_player: ^2.4.5
  passcode_screen: ^2.0.1
  audioplayer: ^0.8.1 #音頻播放器
  audioplayers: ^1.0.1
  audioplayer_web: ^0.7.1
  #audio_recorder: ^1.0.2
  open_app_settings: ^2.0.1
  #flutter_sound: ^9.2.13
  just_audio: ^0.9.27
  just_audio_background: ^0.0.1-beta.7 #支援後台播放
  just_audio_platform_interface: ^4.2.0 #just_audio外掛的通用平臺介面。
  just_audio_mpv: ^0.1.4
  just_audio_windows: ^0.1.0
  just_waveform: ^0.0.3 #波形音訊
  audio_session: ^0.1.8 #通知作業系統音訊現在的性質和狀態
  audio_service: ^0.18.6 #音訊程式，使其在後臺執行或關閉螢幕，並允許您的應用程式與耳機按鈕、Android鎖定螢幕和通知、iOS控制中心、可穿戴裝置和Android Auto進行互動。
  audio_service_web: ^0.1.1
  flutter_cache_manager: ^3.0.1 #可以下載和快取應用程式快取目錄中的檔案。 關於檔案儲存多長時間的各種設定都可以更改。
  android_content_provider: ^0.2.0
  clock: ^1.1.0
  js: ^0.6.3
  flutter_tts: ^3.3.3
  device_info_plus: ^3.2.1
  flutter_sound: ^9.2.13
  flutter_sound_web: 9.2.13
  #flutter_ffmpeg: ^0.3.1
  path: ^1.6.4
  intl: ^0.17.0
  chewie_audio: ^1.3.0
  share_plus: ^4.0.10 #分享
  url_launcher: ^6.1.4 #呼叫
  url_launcher_web: ^2.0.0

  redux: ^5.0.0 #它包括豐富的Docs、中介軟體、Dev Tools生態系統，可以使用flutter_redux軟體包與Flutter結合。
  flutter_redux: ^0.10.0 #一組實用程式，允許您輕鬆使用Redux商店來構建Flutter小部件。
  async: ^2.8.2 #异步的延伸類別 2.9.0和test衝突。
  redux_epics: ^0.15.1 #非常適合響應操作對商店進行同步更新。
  transparent_image: ^2.0.0 #一個簡單的透明影象。 表示為Unit8List，最初是從Flutter程式庫中提取的（在測試包中是私有的）。

  cookie_jar: 3.0.1 #用於http請求的cookie管理器。
  flutter_markdown: ^0.6.10+2 #降價渲染器。它支援原始格式，但不支援內聯HTML。
  badges: ^2.0.3 #徽章
  table_calendar: ^3.0.6
  calendar_view: ^0.0.5
  flutter_colorpicker: ^1.0.3
  vibration: ^1.7.3 #振動
  #in_app_purchase: ^3.0.6 應用程式內購買 註冊需付費。
  marquee: ^2.2.3 #跑馬燈

  extended_text: ^9.1.0
  #extended_text_field: ^10.1.0
  ff_annotation_route_library: ^3.0.0 #支援空安全和非空安全
  oktoast: ^3.2.0
  #loading_more_list: ^4.1.0 #載入更多列表，支援ListView、GridView、WaterfallFlow和Slivers。

  flutter_downloader: ^1.8.0+1 #下載管理
  android_path_provider: ^0.3.0
  device_info: ^2.0.3

  flutter_rating_stars: ^1.0.3+4 #星級評分
  timelines: ^0.1.0 #時間軸
  font_awesome_flutter: ^9.0.0

  intro_slider: ^3.0.9 #前導頁
  sticky_headers: ^0.3.0+2
  azlistview: ^2.0.0 #關鍵字搜尋List
  common_utils: ^2.0.0
  lpinyin: ^2.0.0 #漢字轉拼音
  # github_language_colors
  # url: https://github.com/Sky24n/github_language_colors.git or url: git://github.com/Sky24n/github_language_colors.git
  github_language_colors:
    git:
      url: https://github.com/Sky24n/github_language_colors.git
  scrollable_positioned_list: ^0.2.3 #優化List
  charts_flutter: ^0.12.0 #官方圖表
  palette_generator: ^0.3.3+1 #從影象中提取突出顏色
  flutter_vector_icons: ^1.0.0 #more icons
  flutter_circular_slider: ^2.5.0 #可手動滑動的載入圈
  open_settings: ^2.0.2 #app設定
  reorderables: ^0.5.0 #拖曳List
  simple_gesture_detector: ^0.2.0 #易於使用、可靠和輕量級的手勢檢測器。 暴露簡單的API，以應對基本手勢。
  gzx_dropdown_menu: ^3.1.0 #下拉選單
  search_choices: ^2.0.21 #多種下拉選單
  full_screen_menu: ^1.0.0 #fab
  foldable_sidebar: ^1.0.0 #摺疊型drawer
  flutter_inner_drawer: ^1.0.0+1 #內抽屜
  kumi_popup_window: ^2.0.1 #簡易popup
  solid_bottom_sheet: ^0.1.10 #實心底版
  rubber: ^1.0.1 #彈性底版
  after_layout: ^1.0.7 #即在顯示第一個幀後，帶來執行程式的功能。
  draggable_bottom_sheet: ^0.1.2 #拖曳底版
  pimp_my_button: ^1.0.0 #粒子效果
  confetti: ^0.7.0 #紙屑效果
  argon_buttons_flutter: ^1.1.0 #載入計時器
  flutter_spinkit: ^5.0.0 #動畫Icon
  scroll_to_index: ^3.0.1 #可滾動小部件的固定/可變行高提供了滾動到索引機制。
  flutter_line_sdk: ^2.2.0 #line 登入
  flutter_login_facebook: ^1.5.0+1 #facebook 登入
  story: ^0.7.0 #模仿instagram動態
  dismissible_page: ^0.7.3 #導航到被滑動手勢駁回的頁面，帶有英雄風格的動畫，靈感來自FB，IG故事。
  flutter_webview_plugin: ^0.4.0 #允許Flutter與原生WebView通訊的外掛。
  webview_flutter: ^3.0.4
  flutter_facebook_auth: ^4.4.0+1
  google_fonts: ^3.0.1



  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.2

  #deferred-components:
   # - name: boxComponent
    #  libraries:
     #   - package:flutter_practice2/widget/deferredbox.Dart
dev_dependencies:
  dartdoc: ^5.0.1
  flame_lint: ^0.1.0
  mockito: ^5.0.15
  fake_async: ^1.2.0
  flutter_test:
    sdk: flutter
  flutter_driver:
    sdk: flutter

   # 下面的“flutter_lints”包包含一組推薦的 lints
   # 鼓勵良好的編碼實踐。 包提供的 lint 集是
   # 在位於根目錄的 `analysis_options.yaml` 文件中激活包裹。
   # 有關停用特定 lint 的信息，請參閱該文件規則並激活其他規則。
  flutter_lints: ^2.0.0
  build_runner: ^2.1.11
  build_web_compilers: ^3.2.3
  test: ^1.16.0
  pedantic: ^1.11.0
  coverage: ^1.0.0
  e2e: ^0.2.0
  leancode_lint: ^1.2.1
  innim_lint: ^0.1.7
  #r_upgrade: ^0.3.8+2 自動升級應用程序

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  assets:
    - assets/
    - assets/samples/
    - assets/images/
    - assets/images/process_timeline/
    - assets/data/
    - audio/

  fonts:
    - family: Montserrat
      fonts:
        - asset: fonts/montserrat/Montserrat-Bold.ttf
        - asset: fonts/montserrat/Montserrat-Regular.ttf
    - family: Hind
      fonts:
        - asset: fonts/hind/Hind-Regular.ttf
    - family: RobotoMono
      fonts:
        - asset: fonts/RobotoMono-Regular.ttf
        - asset: fonts/RobotoMono-Bold.ttf
    - family: Raleway
      fonts:
        - asset: fonts/Raleway-Regular.ttf
        - asset: fonts/Raleway-Bold.ttf

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/assets-and-images/#resolution-aware

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/assets-and-images/#from-packages

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/custom-fonts/#from-packages
